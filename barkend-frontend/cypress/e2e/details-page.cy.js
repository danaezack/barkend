describe('User flows for home page', () => {
  beforeEach(() => {
    // cy.intercept('GET', '', {
    //   statusCode: 200,
    //   fixture: 'dogs'
    // })
    cy.visit('http://localhost:3000/')
  })
  
  it('renders dog details page and expected dog details page elements', () => {
    // cy.get()
  })
})